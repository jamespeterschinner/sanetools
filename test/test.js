import { strictEqual } from 'assert'
import {Itertools} from "../index"

describe("range function returns array ",function(){
    it("should return [0,1,2,3,4] when called with range(5)", function(){
        strictEqual([0,1,2,3,4, ])
    })
})